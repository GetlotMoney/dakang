# 需求决策记录

本文件记录当前仍有效并影响后续实施的产品和技术决策。需求状态以 `demo-business-chain-matrix.md` 为准，接口和数据细节以对应契约为准。

## 1. 产品结构

| 编号 | 决策 | 影响范围 |
|---|---|---|
| D-001 | PC 一期采用运营总览、水站、设备、水种套餐、用户、订单中心的 5+1 结构；系统管理保留为基础模块 | REQ-018～REQ-024、REQ-055 |
| D-002 | 主业务菜单一级直达，同一业务域使用页内导航，详情页使用隐藏路由；不保留顶部历史页签和快速入口 | PC 导航与路由 |
| D-003 | PC 负责配置、查询、准入、监控、追溯、裁决和审计，不在后台模拟用户扫码、配送履约或设备执行 | PC 与各业务端职责边界 |
| D-004 | 运营总览保持最小只读范围，完整经营大屏和告警中心在对应业务链实施 | REQ-050、REQ-067 |
| D-005 | 8 种水最终定义、机组层级、设备协议、故障码、微信商户和分账规则属于外部确认项 | 一期外部依赖 |

## 2. 小程序账号与导航

| 编号 | 决策 | 影响范围 |
|---|---|---|
| D-101 | `ws_user` 是唯一账号主体；用水是基础能力，机主和配送能力可以叠加 | REQ-003、REQ-051～REQ-054 |
| D-102 | 小程序固定使用“首页 / 订单 / 我的”三项 Tabbar，不建立角色选择页或三套独立门户 | 小程序信息架构 |
| D-103 | 首页按当前能力和待办展示用水、配送和经营入口；前端入口只负责导航，后端 Service 负责权限和数据范围 | 小程序权限模型 |
| D-104 | 同一账号不能配送自己的订单；可接列表和接单接口都必须校验 | REQ-011、REQ-015、REQ-054 |
| D-105 | 配送签收照片固定为门牌、水品、摆放三类；配送数量和回收数量使用结构化字段 | REQ-016 |

## 3. 水卡与充值

| 编号 | 决策 | 影响范围 |
|---|---|---|
| D-201 | 数据模型允许一个用户拥有多张卡；一期自助首次购卡仅允许当前没有有效水卡的用户进入 | REQ-006、REQ-007 |
| D-202 | 待支付购卡订单不提前建卡；支付事实确认后，在同一事务内完成发卡、入账、流水和订单回填 | 首次购卡事务 |
| D-203 | 首次发卡使用 `ISSUE_ORDER_ID` 唯一锚点；同一订单不能创建第二张卡 | 发卡幂等 |
| D-204 | 套餐 `expireDays` 决定永久或有限；有限新卡有效期从支付成功时间起算，永久卡有效期为 `NULL`。【2026-07-25 修订】有限期机制仅用于活动赠卡模板，正规付费套餐一律 `NULL`（见 D-213） | 水卡有效期 |
| D-205 | 有限卡充值按 `max(currentExpireTime, paySuccessTime) + expireDays` 续期；永久卡保持永久。【2026-07-25 修订】赠卡不可充值（D-213）后本条转为机制保留：公式由真实 MySQL 事务测试钉住，线上无可达路径 | 已有卡充值 |
| D-206 | 套餐范围必须合法；新卡继承下单时的套餐范围快照，已有卡充值要求套餐范围与卡范围一致 | REQ-074、REQ-075 |
| D-207 | 家庭成员通过显式授权使用卡，支持状态、有效期和单日限额；实际使用人和持卡人分别记录 | REQ-009、REQ-057 |
| D-208 | 客户端 `requestId` 使用 UUID；同一用户和请求生成稳定订单，同请求改卡或改套餐时拒绝 | 订单幂等 |
| D-209 | 支付状态主链为待支付 `1` → 已支付待入账 `2` → 完成 `4`；关闭为 `5`，人工处理为 `6`，退款完成为 `7` | 充值状态机 |
| D-210 | 支付窗口为 30 分钟；付款资格由创建时冻结的卡状态、有效期和 `PAY_EXPIRE_TIME` 判断 | 支付资格 |
| D-211 | Pay-Sim 只用于开发和验收环境，生产环境不注册相关入口 | 内部支付模拟 |
| D-212 | 原始支付报文只用于核验、重放和审计，普通页面不展示 openid、签名或完整交易材料 | 支付数据保护 |
| D-213 | 正规渠道付费购买的水卡一律永久有效（余额与水量不过期，预付卡合规底线）；`EXPIRE_DAYS`/`EXPIRE_TIME` 机制仅用于活动赠卡；赠卡不可充值（usable-list `canRecharge=false` + 创单拒绝「活动赠卡不支持充值」）。【2026-08-06 修订】收窄为**有效期内且权益未耗尽时**不可充值；权益用完或到期后按 D-416 条件转正（无主卡可充值转付费卡 / 有主卡合并后作废）；充值赠送金额并入主卡、不设独立期限；赠卡发放入口一期未建（2026-07-25 用户拍板）。【2026-08-06 落码】审计发现本条长期只有文字无守卫（PC 可建出付费+有效期套餐并上架售卖）：现由 `RechargeLimits.validatePackage#rejectPaidLimitedExpiry` 在套餐新增/修改与创单读取处硬拒绝；历史有限期快照仅兼容查询（`validateSnapshotValues` 刻意不加此闸），测试铸造历史数据用 `LegacySnapshots` | 付费权益口径 |
| D-214 | 配送订单支付方式二选一：`payWay=2` 全余额（水费+配送费均扣余额，语义不变）；`payWay=3` 混合结算（水费按容器水量抵扣 `BALANCE_ML`，配送费为服务费仍扣余额，两步同事务、任一失败整体回滚）。水量换算表单一出处 `DeliveryPricing`（3L袋=3000ml、5L桶=5000、10L桶=10000、20L桶=20000，前端同表同步）；金额口径修订（E2E-03 规则2）：`ORDER_AMOUNT`=(payWay=2?水费:0)+配送费，任务行 `WATER_AMOUNT` 同口径，快照新增 payWay/waterMl/priceWaterAmountFen（价目参考）；旧包无 payWay 按 2 兼容，同 requestId 改 payWay 拒绝；自动补货暂仅支持余额（规则表无支付方式列，fail-closed）；赠卡消费不受限（仅充值受 D-213 限制）；补偿边界：payWay=3 单以水量回补为主，待 E2E-04（2026-07-25 用户拍板） | 配送支付口径、REQ-011、REQ-073 |
| D-215 | 后台申诉列表按案件维度（`ws_delivery_appeal.TASK_ID`）聚合分页：一行 = 一个配送任务，同订单多次申诉不再多行。聚合行展示值取代表申诉=活跃申诉（`APPEAL_STATUS=1`，由 `uk_appeal_active_task` 保证至多一条），无活跃则取最近一条（`CREATE_TIME DESC, ID DESC`），故行内 `appealStatus` 即案件当前状态；附带 `appealCount`/`activeAppealId`/`firstAppealTime`/`lastAppealTime`，排序为待处理案件优先、其余按最近申诉时间倒序。状态筛选作用于当前状态，历史上出现过该状态不参与命中（同一案件在多筛选下重复出现更难判读）；`orderNo` 模糊匹配不变。聚合必须在 SQL 层完成（前端聚合会被分页边界切断案件），分页 total 为案件数。数据层零改动：申诉表结构不动、历史行完整保留、裁决仍按单条 `appealId` 走包A 事务。证据详情新增 `appealHistory`（同任务全部申诉正序，含裁决结果、裁决人、裁决时间），共键核验 `linkStatus=mismatch` 时同样不下发；历史条目只出文本与裁决结论，媒体元数据仍只针对当前 `appealId`（2026-07-25 用户拍板） | REQ-017、PC 申诉处理页、E2E-03 包C |

业务上限：

| 字段 | 一期范围 |
|---|---|
| 支付金额 | 1～1,000,000 分 |
| 水量 | 0～50,000,000 ml |
| 赠送金额 | 0～1,000,000 分 |
| 有效期 | `NULL` 或 1～3650 天 |
| 单价 | 非负、最多两位小数、不高于 100,000 分/升 |

## 4. 水配送

| 编号 | 决策 | 影响范围 |
|---|---|---|
| D-301 | 配送订单使用水卡余额支付；水费、配送费、地址、水种和数量在下单时保存快照。【2026-07-25 修订】支付方式扩为二选一（余额 / 水量抵扣+余额付配送费），口径见 D-214；快照语义与金额恒等式同步修订 | REQ-011、REQ-073 |
| D-302 | 配送任务状态为待接单 → 已接单 → 已离站 → 已送达 → 已签收；申诉期间进入申诉状态，裁决后回到终态 | REQ-015～REQ-017 |
| D-303 | 接单、离站、送达、签收必须同时更新任务、订单轨迹、消息和审计时间 | 履约一致性 |
| D-304 | 用户在签收后 24 小时内申诉；申诉、任务和订单必须保持关联一致 | REQ-017、REQ-059 |
| D-305 | 运营裁决可以驳回、登记补送或登记资金补偿；没有实际执行回签时不显示补送、退款或补偿完成 | 售后边界 |

## 4b. 分润 V2（2026-08-06 甲方会议，转写为准）

> 会议中出现的一切数字（10%/8%/5%、5 万、9.8 万、1.5 万、0%/50%/100%）
> **均为讲解示例，不是正式参数**。【2026-08-14 修订】甲方已提供六方分配表并按 D-428 落地，
> 生效计划由管理端整版发布；仍待甲方终确认的数值见 pending-client-confirmations.md C-01。

| 编号 | 决策 | 影响范围 |
|---|---|---|
| D-401 | 同一 ws_user 可同时具备用户、机主、配送员、推荐人、区域服务商多种身份；同一订单中符合多个独立收益角色时各角色分别计算、保留角色明细，入账可归同一收益账户但明细不得合并 | 身份模型、`SplitV2Enum.RoleCode`、`ws_split_component` |
| D-402 | 售水收入与配送费是两条独立分润基数线（WATER_SALE / DELIVERY_FEE），不得合并为同一基数；配送费主要归配送员，平台是否参与由计划配置，机主/推荐人/区域服务商不默认参与配送费 | `SplitPlanCalculator`、计划校验 |
| D-403 | 水费分润按优惠减免后的**实收**计算，不按原价 | 分润基数口径 |
| D-404 | 推荐机主的收益只计一级：任何身份都可推荐机主；A 推 B、B 推 C 时 A 对 C 无收益；禁止无限层级分销。机主加盟推荐关系是独立关系，**不得**复用 `ws_user.REFERRER_USER_ID` 的用户邀请关系 | `WATER_DIRECT_REFERRER`、防传销边界 |
| D-405 | 省/市/区县区域服务商按**级差**分润：各层比例是该层累计权益上限，上级实得=自身累计−最近下级累计；直接归属层拿本层累计全额。绝不把三层比例相加 | `SplitPlanCalculator#regionComponents` |
| D-406 | 经营归属以推荐关系链（血缘）为核心并在建立时冻结；跨行政区放置设备不改变归属，不按地缘重算。计算器输入不含任何行政区字段 | 归属模型 |
| D-407 | 无推荐关系的公域来源默认归公司；后台可**人工**分配给区域服务商（分给区县后省市按级差联动），自动地域分配不实现——「优先分给最近最低层级」只是运营倾向 | `AttributionSource`、公域裁剪 |
| D-408 | 渠道商即省市区县服务商，不另设并列分钱的「渠道商」角色；推荐机主的人是商务行为不是层级身份 | 角色收敛，替代 V1 CHANNEL/REGION 并列预留 |
| D-409 | 普通身份只能推荐机主，不能推荐省市区县服务商；服务商推服务商的一次性奖励与持续收益按甲方待提供的明细表执行 | 推荐资格边界 |
| D-410 | 充值资金先进入公司资金体系；充值时预分（0%/50%/100%/按信用等级）还是消费时分润未最终冻结，退款追偿规则确认前运行态不得开启充值预分润。开发顺序：先做消费时分润主路径（甲方会议认可），预分留作后台可调能力 | 资金安全边界 |
| D-411 | 区域链缺层（如省直招区县、市层无人）时缺失层份额归平台还是归现存上级**未确认**：S1 计算器对非连续前缀链一律拒绝计算（阻断位），待甲方答复后放开。【2026-08-14 修订】阻断位已按 D-428⑤ 放开：缺席层级差切片归最近在场上级，全链缺席才归公司 | `SplitPlanCalculator` 断层语义 |
| D-412 | 分润 V2 采用整版计划模型（`ws_split_plan`/`ws_split_plan_item`）：整版校验、整版生效，禁止各收款方独立选版本；每笔分润落 `ws_split_component` 证据（基数线/基数/角色/比例/金额/计划版本/归属来源），组件键同订单同线同角色唯一。S1 仅交付计算内核与证据表，开关两环境默认 false 未接线。【2026-08-14 修订】S2 已按 D-428③ 接线并开启，六方口径生效 | E2E-08 S1/S2 |
| D-413 | 邀请码与渠道推送码**不分离，只有一个码**：用户扫谁的码即归属谁（维持现有落库口径，零数据清洗）。（2026-08-06 甲方确认） | C-05 关闭、REQ-058 |
| D-414 | 退款范围：**配送费不退，只退水费**——配送服务已履约，退款以水费为限。推论：配送费线（DELIVERY_FEE）分润不存在退款冲减场景。【2026-08-07 落码】`AfterSaleStrategy.compute` 对 SERVICE_FEE_ONLY/PRODUCT_AND_SERVICE 一律拒绝（新裁决无法产生配送费返还），PC 申诉处理页已撤下两策略选项；履约前整单取消（fullRefund）不受影响仍含配送费。`AfterSaleQuota.Caps.capServiceFen` 额度结构保留为 S2 清理项（compute 已挡住入口，额度层不再可达）。水费线已分润部分是否冲减**仍未确认**（见 C-04 余项）。（2026-08-06 甲方确认） | 售后返还边界、E2E-04/E2E-08 |
| D-415 | 赠卡**可以合并**入付费水卡（余额与水量一并转入），但合并权益**保留赠卡原有效期**：实现路径为带 `EXPIRE_TIME` 的权益批次并入目标卡（卡本体仍永久，D-213 不破），界面须展示该部分权益的到期时间；消费顺序沿用既有铁律「最早到期批次优先、永久批次最后」（`EntitlementBatchOrder`），合并权益天然先耗；到期未用完部分按赠卡原语义失效；合并后赠卡本体注销、动作不可逆。【2026-08-07 落码】`/mini/card/merge`（`MiniGiftMergeServiceImpl`）：固定锁序双卡 FOR UPDATE + 批次改挂主卡（携原 EXPIRE_TIME/SCOPE/不可退状态）+ 赠卡清零注销，四条 CAS 同事务；已自然过期赠卡走批次置过期清零、零转移仅注销；幂等锚 `MERGE-OUT:<赠卡ID>` 流水唯一键；目标限**永久**付费卡（有限期存量付费卡拒收，保「卡有效期=批次聚合上界」）；小程序卡详情页提供合并入口与批次到期展示（`expiringBundles`）。双卡账本自检（主卡侧 Σ批次剩余==余额缺一不可）、范围语义不一致 fail-closed 拒绝（批次 SCOPE 尚不参与消费授权）、过期作废统一走 `EntitlementLedger.settleExpired` 唯一清算入口、过期清理响应不下发 main 字段（首次与重放一致）。（2026-08-06 甲方确认，原 C-08） | 赠卡合并、REQ-061、E2E-02/04 |
| D-416 | 赠卡的两条出路（均限**虚拟卡**）：①用户**无付费主卡**时，赠卡权益用完（余额与水量均为 0）或到期后，**该赠卡可以充值**——充值成功即转为正规付费卡（充入权益永久、卡有效期转永久，此后按付费卡语义消费/充值）；②用户**有付费主卡**时，赠卡不走充值，走合并入主卡（D-415）后作废。两条路互斥分流，且都与「一人一张付费卡」现状自洽：转正只发生在无主卡时、合并不新增卡。【2026-08-07 落码】创单口（`MiniRechargeServiceImpl`）按「赠卡=带期且无订单锚」识别，权益耗尽或自然过期且无其他付费卡时放行并铸 `promoteToPermanent` 快照（快照严格白名单新增可选字段，仅 true 时序列化）；入账口（`RechargeCreditTxImpl`）按快照转正：`EXPIRE_TIME→NULL`、状态复位 1，形态不符 unrecoverable；有主卡时拒绝并指路合并（D-415）。转正入账先经 `settleExpired` 作废过期旧权益（终值只含新充值权益）；锁序 payment→order→user→card 与首购同锚 + READ_COMMITTED 锁内名额复查（并发转正/首购恰一张付费卡）；快照记真实卡状态（3 不伪装 1），创单/重放/支付状态查询三处付款窗公式统一按 promote 分流。（2026-08-06 甲方确认，原 C-07） | 赠卡充值转正、REQ-006/007、E2E-02 |
| D-417 | **付费卡一人一张，维持现状口径**（原 C-08.1 选项 A）：一个用户同时只能持有一张付费卡；冻结、过期、注销但未删除的卡同样占用名额并阻断再次购卡；赠卡不占付费卡名额。该口径与 D-416 自洽：赠卡转正只发生在无主卡时、合并不新增卡，任何时刻付费卡至多一张。赠卡本身的持有数量上限**未确认**（现有发卡无数量校验，见 C-08.2）。（2026-08-06 甲方确认） | 一人一卡、REQ-006/007、E2E-02 |
| D-418 | 「0 元注册送」赠卡改为**注册成功自动发放**（不再依赖运营手动发卡）。现状：仅有运营后台手动发放入口（请求号幂等、必带有效期，D-213），注册链无发放挂点，属新增开发；发放参数（余额/水量/有效期天数）未给，见 C-01.8。自动发放的仍是标准活动赠卡，走 D-415/D-416 两条出路，不占付费卡名额（D-417）。【2026-08-12 挂点后移，见 D-426】判据由 `newlyCreated` 改为 `BoundUser.phoneNewlyBound`（本次调用让该账号首次拥有手机号）：游客态下账号在 openid 首登时就建好、`newlyCreated` 恒 false 会让该发的一张都不发，而无条件发又会让幂等复绑与存量老用户首次微信登录白得一张，两种错法方向相反。【2026-08-07 落码】注册建号成功点事务提交后触发 `RegisterGiftServiceImpl.grantIfEnabled`：复用运营发卡唯一入口（同一套 D-213 校验/流水/批次/审计），请求号由 userId type-3 UUID 确定性派生（终身至多一张）；`marketing.register-gift` 配置默认关（改配置需重启生效）、参数非法拒发；发放失败不阻断注册，运营手动补发兜底。参数待 C-01.8。发放事务 READ_COMMITTED（并发重放三证据同视野），重放证据链 fail-closed（批次唯一/快照可解析/天数必比）。（2026-08-07 甲方确认） | 注册送赠卡、E2E-02/E2E-08 |
| D-419 | 配送订单分润基数：**配送费与水费完全分开、各自为线**。现状 V1 挂点基数=整单金额（水费+配送费合计，签收时点），与本决议不一致，属 S2 修正项；S1 已交付的 SplitPlanCalculator 按线建模（与 D-414「配送费线不存在退款冲减」自洽），V2 接线时按分线基数实现。【2026-08-07 落码】签收挂点改调 `enqueueForDeliveryOrder`：水费按 WATER 线、配送费按 DELIVERY 线各自取比例（基数取任务行创单冻结的 waterAmount/deliveryFee），每收款方单行、快照记 "W<比例>+D<比例>" 分线形态，行合计恒等于整单，逐线超配 fail-closed；行级分线模型仍随 V2 接线交付。比例配置写入下沉 Service+商品线锁锚表（ws_split_line_lock）串行化，全未来断点校验。**口径登记**：配送申诉退款目前不冲减已产生的分账（SPLIT_STATUS=4 已回退/REFUND_ID 为休眠扩展位，经甲方确认后启用，见 C-04 水费线冲减未答）——申诉退款后机主/配送员保留全额分润，对账差异仅在退款与账期跑批同期时可检出，属既定待确认范围而非漏报。（2026-08-07 甲方确认） | 分润基数、E2E-08 S2 |
| D-420 | 水费线已分润部分的退款冲减：**全额扣回**（原 C-04 选项 A）——水费退款时，已按实收水费分出去的分润（机主/一级推荐/区域服务商等全部收款方）从各自收益余额按份额扣回，公司不兜底差额；分账回退扩展位激活（ws_split_record SPLIT_STATUS=4已回退 + REFUND_ID），action 为源、REFUND_PRODUCT_FEN 为基数、ws_split_clawback 事实表。落地形态为动作级 outbox 两段式：客户退款事务内只登记一条不可变动作事实（ws_split_clawback_action，ACTION_ID 唯一，零分账读、不可失败）；分摊/校验/明细生成全在独立执行事务，对账不合格整动作转人工、零资金更新；重放走不可变参数等价核验。执行段来源分类：无分账收敛仅限待接单取消/充值退款白名单、来源↔订单类型互核、缺分账/缺平台行/份额为0而退款为正均转人工；收口为统一来源共键校验器（直锚来源 SOURCE_ID==orderId、申诉实体订单/用户/任务三共键当前读、售后号必等于确定性派生、待接单取消零分账收敛前核订单已退款+任务已取消终态）与平台行完整身份三元组（RECEIVER_TYPE 平台+收款方 0+REMAINDER 快照恰一行，伪装/错置/缺失/重复均在动账前整动作转人工，平台行恒不入收益扣回路径）。（2026-08-07 甲方确认） | 退款冲减、E2E-04/E2E-08 |
| D-421 | 分润入账**冻结期**（我方设计，乙方拍板 2026-08-07）：分账行创建满 **1 天**（`settlement.split-freeze-days` 配置化，改配置重启生效）后结算 Worker 才推进入账；冻结期内钱未进收益账户，此窗口内的退款冲减=分账行直接置回退，**零资金动作**——与配送 24h 申诉窗对齐，把 D-420 冲减的负余额问题压缩为「冻结期外残余场景」；残余场景兜底按 C-04.1 选项 B：**不记负余额、限制提现直至差额补足**。机主收益页展示「在途分润」合计与最早解冻时间，防止延迟到账被当漏发。【2026-08-07 落码】Worker 扫描按阈值过滤 + `settleOne` 同口径守卫双保险；钱包 `pendingSplitFen`/`earliestUnfreezeTime` 展示（服务端 VO+小程序）。结算入口数据库事实闭合：`settleOne` 收窄为只收 ID，事务内锁定读（ID+DATA_STATUS=0 FOR UPDATE），冻结判定（CREATE_TIME 严格 14 位 fail-closed）与入账事实（收款人/金额/备注）全取锁内 DB 行，调用方不可伪造；钱包在途聚合下沉 SQL（SUM/MIN，只算金额>0），三轨索引 `idx_split_pending_wallet` 已落档并完成主库迁移。（冻结期机制即刻实现；D-420 冲减链按本设计冻结口径，待排期） | 分润冻结期、E2E-08 |
| D-422 | 二期商城 S0～S3 可执行契约（E2E-09）总纲：平台自营实物商品，SPU 为展示主体、SKU 为定价与库存主体；库存按前置仓×SKU 独立管理，扣减一律原子条件更新、加量走 upsert、流水只增不改；上架三闸=分类启用+存在启用 SKU+启用仓可售>0；小程序只见已上架商品与有货布尔，电话原文与履约范围明细不下发；交易一单一仓（多仓可履约按仓库 ID 升序取第一仓并冻结）、创单事务原子预占且任一不足整单回滚、支付两段式且两段共用唯一准入判据 `MallPayFactGate`；超时关单必须先取得支付方查单 CLOSED；履约七态顺序不可跳、每步精确前态+版本号 CAS、签收只开放给用户、配送归属按结构化证据共键判定。表结构、唯一键、字典与菜单号段、CAS 与恒等式等技术条款以 `docs/contracts/E2E-09-mall-contract.md` 为唯一权威，本表不转录。 | 商城基座、E2E-09 |
| D-423 | 设备高风险指令的**二次验证范围**（我方裁决 2026-08-11，本条为唯一权威口径，其余文档与代码注释只引用编号不复述规则）：REQ-063 验收补充原文只点名「紧急停机和价格类操作需要二次验证和操作日志」，而代码注释一度写成「锁机等高风险操作的二次验证属于商业一期范围」——前者是甲方需求记录（不改），后者把点名的两类放大成「锁机等」又整体推给商业一期（已改）。裁决按**可逆性 / 是否触及计价 / 影响半径**三轴分档，档位钉在 `DeviceEnum.CmdType` 构造器上（新增指令不声明档位即编译不过），判据收敛为唯一实现 `DeviceCommandRisk`：①恒加闸 GATED——紧急停机（平台侧无反向指令、恢复口径待厂家）、价格同步（触及计价，回滚指令改得回价格改不回已按错价成交的订单），任何范围都要二次验证，与需求点名逐字一致；②单台可逆 REVERSIBLE_SINGLE——锁机/解锁/参数同步/重启，逐台点名沿用功能点权限+操作日志+显式确认，范围升到水站或全部才加闸（一次点击让整个机队进入不可用，与单台不是一回事）；③只读 READ_ONLY——查询状态恒不加闸且不因范围升档（只读的范围风险是 MQTT 扇出负载，用目标数上限治）；④出水类 ORDER_ONLY 两条路径永久禁止人工下发。**闸位在 `/device/batch/confirm` 且必须落在凭据 GETDEL 之前**：放在领取之后的话，一次未认证的确认就销毁凭据、逼运营重走预览，那正是二次验证最该避免的假故障；预览段只计算 requireSafe 并冻进凭据+回显给 UI，**预览本身不拦**（预览的价值是让运营先看见「我以为 37 台、实际 41 台」，把口令挡在这个信息前面是本末倒置）；确认段取「冻结值 ∪ 现算值」并集，兼容本次部署前签发的在途凭据。前端走**懒开窗**：先照常确认，撞 1440 才弹口令→`/api/auth/openSafe`→用同一张凭据原样重放；因此不需要对齐安全期(60s)与凭据 TTL(120s)两个窗口，认证底座一个字不改。配套两项：`NotSafeException` 映射为 `ErrorMsg.SAFE_FAIL`(1440) 让前端可判别；管理端 1440 **不计入** IP 限流——懒开窗下「首次确认被拒」是正常流程而非异常行为，计入会让演示时共用出口 IP 的整组人被封禁（与 REPEAT_SUBMIT/PWD_CHANGE_REQUIRED 同型），而真正的口令暴力破解仍在 `/api/auth/openSafe` 抛 JbkException 处正常计数。单发入口 `/device/command/send` 本轮**行为逐位不变**（受理类型仍为 {3,4,5,6,7}），只把手写白名单改成按档位派生，使恒加闸档在结构上进不来。**重开条件（不是遗漏，是挂钩）**：厂家答复真机锁机需现场上电才能解时，LOCK 从 REVERSIBLE_SINGLE 升到 GATED，改档位一处、`DeviceCommandRiskTest` 立即显形。**已登记的残余风险**：范围≥水站的加闸是**代偿**不是范围鉴权——它挡误点与被借用的常驻会话，挡不住有意越权；实质缺口是 `resolveTargets` 对 scopeType=3 全库零归属过滤，范围鉴权（操作人→可控设备集合的判据表）仍待甲方定义。 | 设备中控二次验证、REQ-063、B21 |
| D-424 | 二期商城**多渠道物流履约**基座（E2E-09 L1）核心裁定：「出库物」从履约总单独立为包裹，四表承载（包裹/明细/承运方事实收件箱/出站动作发件箱），不给第三方另开一套表——渠道无关的状态机原语抽为唯一实现共用；渠道互斥判据下沉数据库（承运渠道前态只允许冻结一次）；外呼只在 outbox Worker 里，业务事务只做本地登记；承运方事实两段式，未知事件状态一律转人工不做语义猜测，乱序与迟到只许前进；履约终态恒由用户在小程序确认落定，异常与取消不自动改状态；厂商差异只落适配器。配送端第三方任务完全不可见。**不得表述为「真实第三方物流已接入」或「外部能力已闭环」**；承运方取消运单已建模未实现（口径未定）。完整契约与全部技术条款：`docs/contracts/E2E-09-mall-contract.md` 第十四章。 | 二期商城多渠道物流、E2E-09 L1 |
| D-425 | 小程序**测试登录端点与 Pay-Sim 开关解耦**（我方裁决 2026-08-11）：`/mini/test-login/by-phone` 原随 `mini.pay-sim.enabled` 一起注册，理由是「模拟支付与模拟登录同档次、应同生共死」。正式微信登录验收时实测到后果：演示环境为跑通商城链开了 Pay-Sim，该端点在同一套「正式登录」环境里一并可达，**POST 一个已注册手机号即返回有效 KH_USER Token**——正式登录链旁边始终并行着一条绕过它的路，且没有任何配置项能单独关掉。裁决：两者危险度不同档，必须能分别授权——Pay-Sim 伪造的是带 `PAY_SOURCE=2` 永久标记、可追溯的收款事实；测试登录签出的会话与正式登录完全同形，事后无法从会话本身分辨。改为独立开关 `mini.test-login.enabled`，dev/prod 均缺省 false；隔离验收环境在 `acc-env.sh` 显式置 true （九个 e2e 跑批脚本依赖它按手机号直签会话）。与 refund-sim/pay-sim、mall.pay-sim/mini.pay-sim 的拆分同口径。`SimulationEntryContractTest` 增第三条判据「模拟入口各用各的开关」，注入共用后实测转红。实测证据：解耦前该端点返回 `{"result":"BOUND","tokenValue":"eyJ..."}`，解耦后 `NoResourceFoundException`（本仓异常处理器统一包成 code=500，故不出现 404），Pay-Sim 端点行为不变。 | 正式微信登录、B01、鉴权安全 |
| D-426 | **游客态 + 按需绑号**（甲方口径 2026-08-12，参考美团/淘宝/京东/盒马的通行做法，并与微信《运营规范》15.1.3.1「用户未触发相关功能前不得提前索取个人信息」、15.1.3.2「拒绝授权后不得停止提供全部服务」同向）：小程序保留静默 `wx.login`（无弹窗、不采集个人信息，不受 15.1.3.1 约束），`mini.auth.phoneless-register.enabled=true` 使新 openid 直接建号、`USER_PHONE` 写 NULL——用户观感是游客态，系统内部是完整 KH_USER 主体，因此 **25 个 mini 控制器的 @SaCheckLogin 一个不动**，不引入任何匿名端点这一新攻击面，铁律 6「数据范围按登录人强制过滤」原样成立。**绑号划线**：账号头上落下钱、人或责任三者命中其一即要求手机号——①产生或流出可兑付的钱 ②有人要按这个账号上门或这个账号要上门 ③成为某笔履约/争议的责任主体；附则：收紧方向设闸、放松方向（取消/撤销/暂停自动扣款）不设闸，撤回门槛不得高于发起门槛。**落闸形态**：`MiniPhoneGate` 挂在 **9 处业务原语**（扫码取水扣款/配送扣款/购卡充值/商城下单/商城派单/分润提现/机主报修/一期配送员准入/商城配送员准入），不做 URL 黑名单——黑名单方向是默认放行，新增一个 `create-v2` 就静默漏闸且无测试会红；独立错误码 `PHONE_BIND_REQUIRED(627)` 并豁免 IP 限流计数（社区售水机用户共用出口 IP 是常态，并入 540 会让几十个未绑号用户把整栋楼打进封禁）；判据**实时读库、绝不烤进 Token**（补绑刻意不换会话，烤进去会让用户被自己刚解开的闸继续挡住）。**残留待甲方决策**：存量「有手机号、无 openid」账号在游客态下的认领通道（与换 AppID 后老 openid 全失效叠加），涉及账号合并策略，不是纯技术选择。 | 游客态、绑号闸、B01、D-418 |
| D-427 | PC 一级信息架构按岗位职责收口：订单中心只承载订单查询、配送任务与申诉处理；分账明细、日对账、分账比例配置独立为「财务结算」；自动补货规则、水种用量统计独立为「配送运营」。为保持已投产路径、收藏链接和 RBAC 权限兼容，底层继续使用既有 `/order/*` 路由及菜单 ID，只在 `displayMenuList` 展示树做一根多投影；工作区按实际授权页面裁剪，组内默认页无权限时落到首个授权页，不迁移主库菜单。 | PC 信息架构、权限兼容、一期运营后台 |
| D-428 | **分润六方口径与 V2 接线（S2）**（甲方 2026-08-14 分配表 + 8.6 会议录音双源，本条为唯一权威口径）。①六方=商务推广5% / 水站50% / 运营中心5%（省市区县级差累计，8.6 会议口径省5市3区县2，市与区县两数待甲方明细表终确认）/ 市场激励5% / 公司运营15% / 设备20%，合计恰 100% 且**只覆盖售水线**；配送费线独立（主要归配送员，公司份额可调可关，8.6 会议明确"一定是分开的"）。②后三方（市场激励/公司运营/设备，合计 40%）为**公司内部记账科目**：无收款主体（8.6 会议"到公司只有35块"同构），由平台余数行整块承接，不新铸 receiverType、不伪造收款人；账内 5/15/20 拆分属报表口径（二期）。尾差并入公司余数行（我方默认裁定）。③**接线形态**：分流只在 `SplitServiceImpl` 单入口（完成事务类不感知 V1/V2），开关 `settlement.split-v2.enabled` 两环境显式同值开启；订单创建时点有生效整版计划走 V2，无计划回落 V1——切换期连续性由版本锚（=订单创建时点）保证，计划生效前旧单永远 V1 口径。V2 角色→1377 收款方映射：推荐人(5)/区域服务商(6) 自此被执行面真实消费；行快照形态与冲减链逐字兼容（取水单数字比例、分线单 W/D 前缀、平台恒 REMAINDER 且恒存在）。④**归属载体**：`ws_owner_referrer`（机主加盟推荐关系，D-404 独立关系，一人一行建立即冻结、无换绑入口）与 `ws_owner_attribution`（机主区域归属链，省市区县运营中心三级显式记名，血缘冻结不按地缘 D-406；PUBLIC_MANUAL 承接 D-407 公域人工分配；三级在场者互不相同——同人兼两级会让分账行按(类型,人)聚合产生取整漂移并破坏冲减份额重算，服务层拒绝）。⑤**断层语义（放开 D-411 阻断位）**：直接归属层（最低在场层）拿本层累计全额，其余在场层实得=自身累计−最近在场下级累计；缺席层级差切片被最近在场上级吸收，在场层之上无人时差额落平台，全链缺席才全归公司——与 D-405"最近下级"原文及 8.6 会议"省直招拿全额"同构；前缀链结果与 S1 逐分不变，不再存在因链形态拒算打挂完成动作的路径。⑥**触发时机**：按消费实时分润（8.6 会议甲方明确认可"先消费后分润"方向），充值预分润维持 D-410 关闭态、留作后台可调能力（二期）。实收基数口径不变。⑦V1 侧同轮整改：`configCreate` 收款方白名单（售水=机主、配送=机主/配送员、平台恒拒），封死"配了显示已生效、引擎永不发钱、份额静默落平台"的偏差 3 残留。⑧比例变更维持单人权限+操作日志（双人复核未获需求，不新增审批流）。 | 六方分润、E2E-08 S2、D-404~D-412 承接 |
| D-429 | 同系后台参考图包（2026-08-13 采集，28 图）已按其自述仓库策略在参考价值耗尽后删除；**明确未采纳的参考点**在此留档：①直改余额/积分表单与同页申请审核（图 02/03）——违反资金动作申请/审核/执行分离；②用户云仓库存模型（图 19）——与前置仓×SKU 库存模型冲突且无需求依据；③覆盖式全局比例表单（图 27）——违反整版计划版本化模型（D-412）；④树形关系网络图（图 09）——当前数据无层级权威证明，只做两级只读关系；⑤财务一级菜单不新增——财务页面挂在订单中心下按 D-427 投影为「财务结算」工作区；⑥虚拟销量、代理商套餐等无需求字段一概不引入 | 取长补短边界、LWKM 收尾 |
| D-430 | **小程序经营身份申请与多代理血缘（2026-08-28 用户确认）**：新注册账号默认只有普通用户能力；配送员、机主、渠道推广、区域代理必须在“我的→身份与能力”分别申请，满足绑定手机号、主体、区域、邀请码/经营资产等条件后，演示环境自动审核，正式环境人工审核。能力可在同一 `ws_user` 叠加，首页只显示已通过工作台；固定原生 `首页/订单/我的` Tabbar。一个渠道或区域代理只属于一个区域和一条上级血缘，同一区域允许多个代理竞争；机主按明确邀请码继承“省→市→区→渠道→机主”血缘，无邀请码进入同区域公域池，按最久未分配优先轮转并设24小时响应期。禁止自配送、自我推荐、同一代理血缘重复占据多级；不同真实职责可分别获益。该决策覆盖 D-407“公域只人工分配”和 D-408“渠道不设并列能力入口”的旧产品口径；D-428 的收款角色、级差计算和资金证据模型不变。外部微信支付、物理设备、出金在老板演示档由显式服务器开关模拟，订单、支付、MQTT、分润、提现和审核事实仍真实落库，正式环境缺省关闭。 | B01、E2E-01/03/08、小程序信息架构、身份与血缘 |

## 5. 安全与实施边界

资金与设备安全边界以 `AGENTS.md`「资金与设备安全」铁律为唯一权威口径，本文件不转述；业务链状态与「模拟不构成外部闭环」口径见 `demo-business-chain-matrix.md`。
