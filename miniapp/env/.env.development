NODE_ENV='development'
VITE_DELETE_CONSOLE='false'

# 2026-08-02 mock 基建退役：开发档同样全域接真（mock 适配器已删除，无兜底实现）。
# 本机后端缺省 localhost:13330；真机联调与真实 AppID 写 .env.development.local（不入库）。
VITE_SERVER_BASEURL='http://localhost:13330/dakangApi'
VITE_API_MODE_AUTH='real'
VITE_API_MODE_DEVICE='real'
VITE_API_MODE_ORDER='real'
VITE_API_MODE_CARD='real'
VITE_API_MODE_RECHARGE='real'
VITE_API_MODE_DELIVERY='real'
VITE_API_MODE_MESSAGE='real'

# 测试登录默认关闭：配置手机号才会出现测试账号入口（仅隔离/验收环境使用）。
VITE_TEST_LOGIN_PHONE=''
